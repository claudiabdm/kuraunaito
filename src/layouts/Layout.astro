---
import "@fontsource/roboto-slab";
import "../styles/global.css";
import TheHeader from "../components/TheHeader.astro";
import Meta from "../components/Meta.astro";

const { title, slug, lang, translatedSlug, seo } = Astro.props;
---

<!DOCTYPE html>
<html lang={lang}>
  <head>
    <Meta seo={seo} title={title} />
  </head>
  <body class="body">
    <TheHeader {...{slug, lang, translatedSlug}} />
    <main class="main">
      <slot />
    </main>
    <style lang="scss">
      .body {
        max-width: 1040px;
        margin-left: auto;
        margin-right: auto;
        padding: var(--space-xs-s) var(--space-s-l);
        color: var(--primary);
        font-size: var(--step-0);
      }
    </style>
  </body>
</html>
